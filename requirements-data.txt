# =============================================================================
# F1_PITSTOP_RL — dépendances de COLLECTE des données (hors image Docker)
# =============================================================================
# Uniquement pour régénérer les Parquet depuis l'API FastF1.
# Le service d'inférence n'en a pas besoin : les Parquet sont déjà produits.
#
# Environnement SÉPARÉ (.venv-data), ne pas installer dans .venv312 :
#   - fastf1 3.8.x exige pandas<3, l'inférence est figée en pandas 3.0.6 ;
#   - les versions antérieures (3.0.2, 3.3.0) ne supportent pas numpy 2.
# Aucune version de fastf1 ne peut donc cohabiter avec l'inférence.
# L'interface entre les deux environnements est le fichier Parquet.
#
# Les Parquet versionnés ont été générés avec une version antérieure de fastf1.
# Environnement complet figé : requirements-data.lock.txt
# =============================================================================
fastf1==3.8.3
pyarrow==25.0.1
